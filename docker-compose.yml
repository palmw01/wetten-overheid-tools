version: "3.9"

services:
  mcp-debugger:
    image: ghcr.io/palmw01/wetten-overheid-tools:latest
    container_name: mcp-debugger
    restart: unless-stopped
    environment:
      TZ: Europe/Amsterdam
    security_opt:
      - no-new-privileges:true
    ports:
      - "3001:3000"
    volumes:
      - /volume1/docker/wetten-overheid-tools/config:/app/config
      - /volume1/docker/wetten-overheid-tools/logs:/app/logs
    networks:
      - internal

  quartz-site:
    image: ghcr.io/palmw01/wetten-overheid-tools-quartz:latest
    container_name: quartz-site
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "3002:3000"
    networks:
      - internal

networks:
  internal:
    external: true
    name: homeinfra_internal